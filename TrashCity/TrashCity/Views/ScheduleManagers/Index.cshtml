@model IEnumerable<TrashCity.Models.ScheduleManager>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.customer.CustomerFirstName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.dateToChange)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.tempCollectionDay)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.customer.CustomerFirstName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.dateToChange)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tempCollectionDay)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CustomerId }) |
            @Html.ActionLink("Details", "Details", new { id=item.CustomerId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CustomerId })
        </td>
    </tr>
}

</table>
